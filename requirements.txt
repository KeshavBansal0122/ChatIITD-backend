# Core API dependencies
aiofiles>=24.1.0
fastapi>=0.116.1
uvicorn[standard]>=0.35.0
python-dotenv>=1.1.1
python-multipart>=0.0.20
httpx==0.28.1

# OpenAI SDK (used for OpenRouter)
openai>=1.102.0

# Database
SQLAlchemy==2.0.29
sqlmodel==0.0.27
psycopg2-binary>=2.9.9  # PostgreSQL driver
qdrant-client==1.10.1

# Authentication
python_jose==3.5.0

# PDF processing and chunking (still needed for document upload)
pymupdf4llm>=0.0.27
pypdf>=6.0.0
unstructured>=0.18.14

# Gradio UI (optional, for standalone app.py)
gradio>=5.47.2

# Data processing
pandas>=2.3.2

# Development
ipykernel>=6.30.1

# Optional - for compatibility with other parts of the codebase
# Remove these if not used elsewhere:
# anthropic>=0.64.0
# litellm>=1.76.0
# tiktoken>=0.11.0

# Embeddings for document upload / Qdrant (used by qdrant_service + chunking/)
sentence-transformers>=5.1.0
torch>=2.8.0
transformers>=4.55.4

# NOTE: Langchain dependencies removed - agent now uses OpenRouter directly
# If you need Langchain for other parts, add them back:
# langchain==0.3.27
# langchain-community==0.3.29
# langchain-google-genai==2.1.12
# langchain-huggingface==0.3.1
# langchain-qdrant>=0.2.0
# langchain-text-splitters>=0.3.9

# Wiki integration
mwclient>=0.11.0